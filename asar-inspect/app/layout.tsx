laceholder,
.entry-field input::placeholder {
  color: var(--subtle);
}

.text-count {
  position: absolute;
  right: 10px;
  bottom: 8px;
  color: var(--subtle);
  font-size: 11px;
  font-weight: 800;
}

.entry-note {
  flex: 0 0 auto;
  margin: 0;
  border: 1px solid #dbe7ee;
  border-radius: 7px;
  background: #f7fbfc;
  padding: 8px 10px;
  color: var(--muted);
  font-size: 12px;
  line-height: 1.4;
}

.entry-g